<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thank You - Check Your Email | MikeGuides</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            color: #1a202c;
            background: linear-gradient(135deg, #1e40af 0%, #6b7280 50%, #374151 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .thank-you-container {
            max-width: 800px;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border-radius: 24px;
            padding: 60px 40px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.1);
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.2);
            animation: slideInUp 0.8s ease;
        }

        @keyframes slideInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .logo {
            color: #1e40af;
            font-size: 2rem;
            font-weight: 900;
            margin-bottom: 20px;
        }

        .success-icon {
            background: linear-gradient(135deg, #6b7280, #374151);
            color: white;
            width: 100px;
            height: 100px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            margin: 0 auto 30px;
            box-shadow: 0 20px 40px rgba(107, 114, 128, 0.3);
            animation: bounce 1s ease infinite;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-10px);
            }
            60% {
                transform: translateY(-5px);
            }
        }

        .thank-you-title {
            color: #1f2937;
            font-size: 3rem;
            font-weight: 800;
            margin-bottom: 20px;
            background: linear-gradient(135deg, #1e40af, #374151);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .thank-you-subtitle {
            color: #4b5563;
            font-size: 1.5rem;
            margin-bottom: 40px;
            font-weight: 500;
        }

        .next-steps {
            background: #f9fafb;
            border-radius: 16px;
            padding: 30px;
            margin: 40px 0;
            text-align: left;
        }

        .next-steps h3 {
            color: #1f2937;
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 20px;
            text-align: center;
        }

        .step-item {
            display: flex;
            align-items: flex-start;
            margin-bottom: 20px;
            padding: 15px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        }

        .step-number {
            background: linear-gradient(135deg, #1e40af, #3730a3);
            color: white;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            margin-right: 15px;
            flex-shrink: 0;
        }

        .step-content {
            flex: 1;
        }

        .step-title {
            font-weight: 600;
            color: #1f2937;
            margin-bottom: 5px;
        }

        .step-description {
            color: #6b7280;
            font-size: 0.95rem;
        }

        .bonus-section {
            background: linear-gradient(135deg, #1e40af, #3730a3);
            color: white;
            padding: 30px;
            border-radius: 16px;
            margin: 30px 0;
        }

        .bonus-title {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 15px;
        }

        .bonus-list {
            list-style: none;
            padding: 0;
        }

        .bonus-item {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
            font-size: 1.1rem;
        }

        .bonus-icon {
            margin-right: 12px;
            font-size: 1.2rem;
        }

        .cta-section {
            background: linear-gradient(135deg, #374151, #1f2937);
            color: white;
            padding: 40px;
            border-radius: 16px;
            margin: 30px 0;
        }

        .cta-title {
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 15px;
        }

        .cta-description {
            margin-bottom: 25px;
            opacity: 0.95;
        }

        .cta-button {
            background: linear-gradient(135deg, #1e40af, #3730a3);
            color: white;
            border: none;
            padding: 18px 40px;
            border-radius: 12px;
            font-size: 1.2rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            text-transform: uppercase;
            letter-spacing: 1px;
            box-shadow: 0 10px 25px rgba(30, 64, 175, 0.4);
        }

        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(30, 64, 175, 0.5);
        }

        .footer-note {
            color: #6b7280;
            font-size: 0.9rem;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #e5e7eb;
        }

        @media (max-width: 768px) {
            .thank-you-container {
                padding: 40px 25px;
            }
            
            .thank-you-title {
                font-size: 2.2rem;
            }
            
            .thank-you-subtitle {
                font-size: 1.2rem;
            }
            
            .next-steps {
                padding: 20px;
            }
            
            .step-item {
                flex-direction: column;
                text-align: center;
            }
            
            .step-number {
                margin-bottom: 10px;
                margin-right: 0;
            }
        }

        .pulse-animation {
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.05);
            }
            100% {
                transform: scale(1);
            }
        }
    </style>
</head>
<body>
    <div class="thank-you-container">
        <div class="logo">MikeGuides</div>
        <div class="success-icon">✓</div>
        
        <h1 class="thank-you-title">Success!</h1>
        <p class="thank-you-subtitle">Check your email for instant access to your advanced pricing tools</p>
        
        <div class="next-steps">
            <h3>What Happens Next:</h3>
            
            <div class="step-item">
                <div class="step-number">1</div>
                <div class="step-content">
                    <div class="step-title">Check Your Email (Next 5 Minutes)</div>
                    <div class="step-description">Look for "Your Advanced Pricing Tools Are Ready" from MikeGuides</div>
                </div>
            </div>
            
            <div class="step-item">
                <div class="step-number">2</div>
                <div class="step-content">
                    <div class="step-title">Download Your Bonus Materials</div>
                    <div class="step-description">Advanced calculators, templates, and strategies - everything you need to price profitably</div>
                </div>
            </div>
            
            <div class="step-item">
                <div class="step-number">3</div>
                <div class="step-content">
                    <div class="step-title">Start Using Immediately</div>
                    <div class="step-description">Apply the tools to your next quote and see the difference in your profit margins</div>
                </div>
            </div>
        </div>

        <div class="bonus-section">
            <div class="bonus-title">🎁 Your FREE Bonus Package Includes:</div>
            <ul class="bonus-list">
                <li class="bonus-item">
                    <span class="bonus-icon">🧮</span>
                    Advanced Cost Analysis Templates
                </li>
                <li class="bonus-item">
                    <span class="bonus-icon">📊</span>
                    Competitive Pricing Research Guide
                </li>
                <li class="bonus-item">
                    <span class="bonus-icon">💬</span>
                    Client Objection Handling Scripts
                </li>
                <li class="bonus-item">
                    <span class="bonus-icon">📈</span>
                    Monthly Pricing Strategy Updates
                </li>
                <li class="bonus-item">
                    <span class="bonus-icon">📋</span>
                    Professional Quote Templates
                </li>
            </ul>
        </div>

        <div class="cta-section pulse-animation">
            <div class="cta-title">Want More Interactive Tools?</div>
            <p class="cta-description">Explore more calculators and guides that save you time and money in your business.</p>
            <a href="/" class="cta-button">Explore More Tools</a>
        </div>

        <div class="footer-note">
            <p><strong>Can't find the email?</strong> Check your spam folder and add hello@mikeguides.co to your contacts.</p>
            <p style="margin-top: 10px;">Questions? Reply to any of our emails and we'll help you get started.</p>
        </div>
    </div>

    <script>
        // Track page view
        console.log('Thank you page viewed at:', new Date());
        
        // Track time on page
        let startTime = Date.now();
        window.addEventListener('beforeunload', function() {
            let timeSpent = Date.now() - startTime;
            console.log('Time spent on thank you page:', Math.round(timeSpent/1000), 'seconds');
        });

        // Add some interactive elements
        document.addEventListener('DOMContentLoaded', function() {
            // Animate step items
            const stepItems = document.querySelectorAll('.step-item');
            stepItems.forEach((item, index) => {
                setTimeout(() => {
                    item.style.opacity = '0';
                    item.style.transform = 'translateX(-20px)';
                    item.style.transition = 'all 0.5s ease';
                    
                    setTimeout(() => {
                        item.style.opacity = '1';
                        item.style.transform = 'translateX(0)';
                    }, 100);
                }, index * 200);
            });
        });
    </script>
</body>
</html>
